[package]
name = "nebula-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Nebula memory database: shared types, binary codec and errors"

[dependencies]
